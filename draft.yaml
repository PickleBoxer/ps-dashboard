models:
  Store:
    name: string
    description: string nullable
    url: string
    relationships:
      hasOne: Team

  Team:
    name: string
    description: string nullable
    relationships:
      hasOne: Store
      belongsToMany: User
