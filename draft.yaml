models:
  Store:
    name: string
    description: string nullable
    url: string
    relationships:
      hasOne: Team

  Team:
    name: string
    description: string nullable
    store_id: id foreign
    relationships:
      belongsToMany: User
