models:
  Store:
    name: string
    description: string nullable
    url: string
    active: boolean
    relationships:
      belongsToMany: Employee

  Employee:
    name: string
    email: string
    notes: text nullable
    active: boolean
    relationships:
      belongsToMany: Store
