models:
  Store:
    name: string
    description: string nullable
    url: string
    relationships:
      belongsTo: Team

  Team:
    name: string
    description: string nullable
    relationships:
      hasOne: Store
      belongsToMany: User
